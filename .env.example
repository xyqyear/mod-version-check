# Application
APP_NAME=Mod Version Checker
DEBUG=false

# Database
DATABASE_URL=sqlite+aiosqlite:///./data/db.sqlite3

# CORS (comma-separated origins are not supported — use JSON array in code)
# Defaults: http://localhost:5173, http://localhost:3000
